package com.example.datastruture.binarysearch;

/**
 * 查找第一个大于等于给定值的元素
 */
public class BinarySearchStartGTValue {
    public void bGTStartSearch(int[] array, int value) {
        int low = 0;
        int high = array.length - 1;
        while (low <= high) {
            int mid = low + ((high - low) >> 1);
            if (array[mid] >= value) {
                if ()
            }
        }
    }
}
